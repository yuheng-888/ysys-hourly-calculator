<UserControl x:Class="AutoSoundTimeV3.Views.ManualModeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:AutoSoundTimeV3.Models">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="10" BorderBrush="#E5E5EA" BorderThickness="1" CornerRadius="6">
            <WrapPanel VerticalAlignment="Center">
                <TextBlock Text="智能输入" VerticalAlignment="Center" />
                <TextBox Text="{Binding SmartInput, UpdateSourceTrigger=PropertyChanged}" Width="120" Margin="6,0,10,0" />

                <TextBlock Text="时" VerticalAlignment="Center" />
                <TextBox Text="{Binding Hours, UpdateSourceTrigger=PropertyChanged}" Width="50" Margin="6,0,10,0" />

                <TextBlock Text="分" VerticalAlignment="Center" />
                <TextBox Text="{Binding Minutes, UpdateSourceTrigger=PropertyChanged}" Width="50" Margin="6,0,10,0" />

                <TextBlock Text="秒" VerticalAlignment="Center" />
                <TextBox Text="{Binding Seconds, UpdateSourceTrigger=PropertyChanged}" Width="50" Margin="6,0,10,0" />

                <TextBlock Text="方式" VerticalAlignment="Center" />
                <ComboBox SelectedValue="{Binding Method}" SelectedValuePath="Tag" Width="120" Margin="6,0,10,0">
                    <ComboBoxItem Content="时薪计算" Tag="{x:Static models:CalculationMethod.Hourly}" />
                    <ComboBoxItem Content="分钟计算" Tag="{x:Static models:CalculationMethod.Minute}" />
                    <ComboBoxItem Content="手动金额" Tag="{x:Static models:CalculationMethod.Manual}" />
                </ComboBox>

                <TextBlock Text="费率" VerticalAlignment="Center" />
                <TextBox Text="{Binding Rate, UpdateSourceTrigger=PropertyChanged}" Width="80" Margin="6,0,10,0" />

                <TextBlock Text="手动金额" VerticalAlignment="Center" />
                <TextBox Text="{Binding ManualAmount, UpdateSourceTrigger=PropertyChanged}" Width="80" Margin="6,0,10,0" />

                <Button Content="添加" Command="{Binding AddEntryCommand}" Padding="10,6" Margin="6,0,0,0" />
                <Button Content="清空" Command="{Binding ClearCommand}" Padding="10,6" Margin="6,0,0,0" />
            </WrapPanel>
        </Border>

        <ListView Grid.Row="1" ItemsSource="{Binding Entries}" Margin="0,10,0,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="时长" Width="120" DisplayMemberBinding="{Binding FormattedTime}" />
                    <GridViewColumn Header="费率" Width="80" DisplayMemberBinding="{Binding Rate}" />
                    <GridViewColumn Header="金额" Width="80" DisplayMemberBinding="{Binding Salary}" />
                </GridView>
            </ListView.View>
        </ListView>

        <Border Grid.Row="2" Padding="10" BorderBrush="#E5E5EA" BorderThickness="1" CornerRadius="6">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="合计" Margin="0,0,6,0" />
                <TextBlock Text="{Binding TotalAmountText}" FontWeight="Bold" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
